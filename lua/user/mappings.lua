local map = require("utils").map

local user_mappings = {
	lsp = {
		log = { keybind = "<leader>lp" },
	},
	poop = {
		yeet = { keybind = "!!" },
	},
	nvim_tree = {},
	telescope = {},
}

return user_mappings
